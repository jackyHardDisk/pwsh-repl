# Wrapper Servers

This directory contains MCP server wrappers.

Wrapper servers act as MCP clients to downstream servers and MCP servers to Claude. They
are useful for exposing a subset of a larger toolset, which can lead to significant
token savings.
