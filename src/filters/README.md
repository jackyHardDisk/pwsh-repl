# Filter Servers

This directory contains MCP server filters.

Filter servers are used to optimize token usage by filtering the data that is sent to
the model. This can result in significant token savings, as demonstrated by the examples
in the root README.md.
